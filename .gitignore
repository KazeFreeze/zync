# dependencies / build
node_modules/
dist/
build/
*.tsbuildinfo

# local Obsidian dev-loop scratch (server snapshots, headless peer vault) — see
# docs/superpowers/notes/2026-06-17-zync-m1-dev-loop-runbook.md
.zync-dev/

# logs / os
*.log
.DS_Store

# editor / local / sandbox-mounted device dotfiles (not real project files)
.claude/
.idea
.vscode
.bashrc
.bash_profile
.profile
.zshrc
.zprofile
.gitconfig
.gitmodules
.ripgreprc
.mcp.json
