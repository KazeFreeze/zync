{
  "name": "@zync/idb-bench",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Headless-Chromium IndexedDB benchmark: stock y-indexeddb (one DB/doc) vs single-DB KV.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "bench": "node build.mjs && node runner.mjs"
  },
  "dependencies": {
    "idb": "^8.0.3",
    "y-indexeddb": "^9.0.12",
    "yjs": "^13.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.2",
    "playwright": "^1.54.0",
    "typescript": "^5.6.0"
  }
}
