# Alpha (DIVERGENT local draft)

This is device B's DIVERGENT pre-onboarding copy of notes/alpha.md. Its body
differs from the base `mini` fixture's alpha.md, so when B starts sync against a
relay that already holds A's alpha.md the bootstrap routes to a supervised
import: B adopts A's server copy as the live note and PARKS this local draft as
a conflict artifact (never a silent overwrite, never a blended merge3).

STATUS: divergent-local-draft

End of B's divergent draft.
