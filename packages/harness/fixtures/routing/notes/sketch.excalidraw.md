# Sketch

An Excalidraw-in-markdown note. Despite the embedded drawing block, the file
extension is `.md` and the content is valid UTF-8 well under the prose cap, so
`classify.ts` routes it to `crdt-prose` (NOT a blob). This fixture pins that
real route.

## Drawing

```compressed-json
N4Igsynthetic-not-real-excalidraw-payload-placeholder==
```

%%
## Text Elements
synthetic excalidraw text element ^id-1
%%
